# Mapbox GL JS public access token. Get a free one at https://account.mapbox.com/
# pk.* tokens are public-by-design (Vite bakes them into the bundle), but you
# should still scope yours via the URL allowlist in the Mapbox dashboard.
VITE_MAPBOX_TOKEN=pk.your_mapbox_public_token_here
